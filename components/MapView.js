import React, { useState } from "react";
import { FlatList, StyleSheet } from "react-native";
import { Text, View, Text } from "react-native-ui-lib";


export const MapView = ({

}) => {
  return (
    <View style={styles.root}>
      <Text>dsfsd</Text>
    </View>
  );
}

const styles = StyleSheet.create({

});
